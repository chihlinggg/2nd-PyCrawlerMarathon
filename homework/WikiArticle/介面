[<p>在中國大陸，port的中文譯名為「端口」或「<b>接口</b>」。interface的中文譯名為「界面」或「<b>接口</b>」。
</p>, <p>在台灣，port的中文譯名為「埠」，interface的中文譯名為「介面」。
</p>, <p>當<b>介面</b>是<b>port</b>或<b>socket</b>的譯名時，可以指：
</p>, <p>當<b>介面</b>是<b>interface</b>的譯名時，可以指：
</p>]